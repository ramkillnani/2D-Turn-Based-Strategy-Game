using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Mage : CharacterController
{
    // set ranged attack here
    // Start is called before the first frame update
    void Start()
    {
        
    }
    //override the combat so that it can be activated not based on proximity but instead based on a rnage value
    //override the pathfinding so that the unit stops and fires when it is in range
    // Update is called once per frame
    void Update()
    {
        
    }
}
