using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Movement : MonoBehaviour
{
    //designate a max movement and cur movement available
    //create costs moving into a map tile based on which terrain type
    //extend that to other tiles
    //set active area highlights you can go based on cur movement remaining and on enemies based
    //write a pathfinding algorithm
    //have an UI component to indicate what route the unit wil move to get to the space 
    //set easy terrain to 1 cost,difficult to 2 cost and impassable terrain to 9001 cost
        //make Flyers set all terrain cost to 1
    //make unit switch to combat rather then moving when landing on an enemy square
    // Start is called before the first frame update
    void Start()
    {
        
    }
    // reset movement to max at start of turn
    // Update is called once per frame
    void Update()
    {
        
    }
}
