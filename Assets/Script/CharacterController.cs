using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharacterController : MonoBehaviour
{
    //reference a slider here and call it health bar
    //set floats for curHealth, curMovement, base attack
    //set bool for flying
    // Start is called before the first frame update
    //Set a movement script and a combat to this.
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //when slider value does not equal curhealth divided by maxhealth change it so it equals
    }
}
