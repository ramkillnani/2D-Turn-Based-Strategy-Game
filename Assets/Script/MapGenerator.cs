using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MapGenerator : MonoBehaviour
{
    //set an array of prefab terrain (to increase frequency add a prefab multiple times)
    //set a grid of empty hexagonal spaces to fill in with the prefabs

    // Start is called before the first frame update
    void Start()
    {
        //for each empty spaces instantiate terrain from array.
        //use a randomizer in the range of terrain.length to decide which terrain to spawn at each point
            
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
